services:
  invforge:
    build: .
    ports:
      - "5020:502"
      - "8080:8080"
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://127.0.0.1:8080/health"]
      interval: 2s
      timeout: 2s
      retries: 5
      start_period: 5s
